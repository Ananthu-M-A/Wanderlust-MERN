const botName: string = 'WanderLust ChatBot 1.0.0';


const config = {
    botName: botName,
    customStyles: {
        botMessageBox: {
            backgroundColor: '#2563EB',
        },
        chatButton: {
            backgroundColor: '#DC2626',
        },
    },
};

export default config;
